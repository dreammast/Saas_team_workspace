server:
  port: ${PORT:8080}

spring:
  application:
    name: projectflow-backend
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:your-email@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: ${SPRING_DATASOURCE_URL:${DATABASE_URL:jdbc:postgresql://localhost:5432/projectflow}}
    username: ${SPRING_DATASOURCE_USERNAME:${DATABASE_USERNAME:postgres}}
    password: ${SPRING_DATASOURCE_PASSWORD:${DATABASE_PASSWORD:postgres}}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  cache:
    type: ${SPRING_CACHE_TYPE:simple}

projectflow:
  jwt:
    secret: ${JWT_SECRET:407780T00Y587123984712034981723491823491823498172349817239487123}
    expiration-ms: 86400000 # 24 hours
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}

